#pragma once

#include <string>
#include <vector>

// CONST
const std::string resources_dir = "resources/";
const float ZOOM_SCALE = 1.2;

const float MIN_COMPLEX_REAL = -1.0;
const float MAX_COMPLEX_REAL = 1.0;
const float MIN_COMPLEX_IMAGE = -1.0;
const float MAX_COMPLEX_IMAGE = 1.0;
const float MIN_RADIUS = 1;
const float MAX_RADIUS = 100;
const int MIN_ITERATIONS = 1;
const int MAX_ITERATIONS = 500;
// ^^^ CONST
